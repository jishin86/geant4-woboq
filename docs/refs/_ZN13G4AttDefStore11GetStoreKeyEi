<dec f='codebrowser/source/graphics_reps/include/G4AttDefStore.hh' l='46' type='G4bool G4AttDefStore::GetStoreKey(const int )'/>
<doc f='codebrowser/source/graphics_reps/include/G4AttDefStore.hh' l='40'>// Returns a pointer to the definitions accessed by the given key.
  // &quot;isNew&quot; is true if definitions pointer is new and therefore the
  // needs filling.  The store keeps the ownership of the returned
  // pointer.  See G4Trajectory::GetAttDefs for an example of the use
  // of this class.</doc>
