<def f='codebrowser/source/intercoms/include/G4UImanager.hh' l='193' ll='194' type='void G4UImanager::SetPauseAtBeginOfEvent(G4bool vl)'/>
<doc f='codebrowser/source/intercoms/include/G4UImanager.hh' l='184'>//  These six methods returns the current value of a parameter of the
      // given command. For the first three methods, the ordering number of
      // the parameter (1 is the first parameter) can be given, whereas,
      // other three methods can give the parameter name.
      //  If &quot;reGet&quot; is true, actual request of returning the current value
      // will be sent to the corresponding messenger, while, if it is false,
      // the value stored in G4Umanager will be used. The later case is valid
      // for the sequential invokation for the same command.</doc>
<use f='codebrowser/source/run/src/G4RunMessenger.cc' l='487' u='c' c='_ZN14G4RunMessenger11SetNewValueEP11G4UIcommand8G4String'/>
