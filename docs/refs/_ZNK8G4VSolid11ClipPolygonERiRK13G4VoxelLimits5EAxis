<dec f='codebrowser/source/geometry/management/include/G4VSolid.hh' l='296' type='void G4VSolid::ClipPolygon(int &amp; pPolygon, const G4VoxelLimits &amp; pVoxelLimit, const EAxis pAxis) const'/>
<doc f='codebrowser/source/geometry/management/include/G4VSolid.hh' l='285'>// Calculate the maximum and minimum extents of the polygons
      // joining the CrossSections at pSectionIndex-&gt;pSectionIndex+3 and
      //                              pSectionIndex+4-&gt;pSectionIndex7
      // in the List pVertices, within the boundaries of the voxel limits
      // pVoxelLimit.
      //
      // If the minimum is &lt;pMin pMin is set to the new minimum.
      // If the maximum is &gt;pMax pMax is set to the new maximum.
      //
      // No modifications are made to pVertices.</doc>
