<dec f='codebrowser/source/geometry/volumes/include/G4AssemblyVolume.hh' l='208' type='unsigned int'/>
<use f='codebrowser/source/geometry/volumes/include/G4AssemblyVolume.icc' l='36' u='r' c='_ZNK16G4AssemblyVolume16GetImprintsCountEv'/>
<use f='codebrowser/source/geometry/volumes/include/G4AssemblyVolume.icc' l='42' u='w' c='_ZN16G4AssemblyVolume16SetImprintsCountEj'/>
<use f='codebrowser/source/geometry/volumes/include/G4AssemblyVolume.icc' l='49' u='w' c='_ZN16G4AssemblyVolume17ImprintsCountPlusEv'/>
<use f='codebrowser/source/geometry/volumes/include/G4AssemblyVolume.icc' l='55' u='w' c='_ZN16G4AssemblyVolume18ImprintsCountMinusEv'/>
<offset>0</offset>
<doc f='codebrowser/source/geometry/volumes/include/G4AssemblyVolume.hh' l='198'>//
    // We need to keep list of physical volumes created by MakeImprint() method
    // in order to be able to cleanup the objects when not needed anymore.
    // This requires the user to keep assembly objects in memory during the
    // whole job or during the life-time of G4Navigator, logical volume store
    // and physical volume store keep pointers to physical volumes generated by
    // the assembly volume.
    // When an assembly object is about to die it will destroy all its
    // generated physical volumes and rotation matrices as well !</doc>
