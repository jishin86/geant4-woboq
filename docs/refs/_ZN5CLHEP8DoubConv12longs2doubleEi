<dec f='codebrowser/source/externals/clhep/include/CLHEP/Random/DoubConv.h' l='45' type='static double CLHEP::DoubConv::longs2double(const int )'/>
<doc f='codebrowser/source/externals/clhep/include/CLHEP/Random/DoubConv.h' l='39'>// longs2double (v) returns a double containing the value represented by its  
  // input, which must be a vector containing 2 unsigned longs.  
  // The input is taken to be the representation according to
  // the IEEE 754 standard for a 64-bit floating point number, whose value
  // is returned as a double.  The byte-ordering of the double result is, 
  // of course, tailored to the proper byte-ordering for the system.</doc>
